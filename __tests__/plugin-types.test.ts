import { describe, it, expect } from "vitest"

// Simple test to verify plugin types are valid
describe("plugin-types", () => {
  it("should export AgentLoopPlugin types correctly", () => {
    // This test validates that the plugin type declarations are syntactically correct
    expect(true).toBe(true)
  })
})
