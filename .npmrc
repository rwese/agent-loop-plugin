# Codeberg npm registry configuration
# This file ensures packages are fetched from the correct registry

# Set Codeberg as the registry for @nope-at scope
@nope-at:registry=https://codeberg.org/api/packages/npm

# Use Codeberg for publishing (if needed)
# registry=https://codeberg.org/api/packages/npm

# Ensure we don't require auth for public packages
# npm always needs auth for publishing, but not for installing public packages
